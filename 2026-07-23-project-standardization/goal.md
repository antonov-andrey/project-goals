# Стандартизировать project tooling во всех проектах

## Outcome

Привести 18 existing repositories под `/home/andrey/Projects` и новый `antonov-andrey/project-standards`, всего 19 final canonical repositories, к единой harness-neutral модели project instructions, stable design и временных `.spec/` artifacts; атомарно переименовать `antonov-andrey/workflow-container-tools` в `antonov-andrey/agent-plugins` с заменой всех active Git URLs и переименованием всех live local copies согласно их repository или plugin identity; завершить оставшийся exact Docker cleanup retired `compose-codex-app-server`; отвязать шесть consumers и удалить retiring `antonov-andrey-org/template-bin`, сохранив их runtime files как независимые project-local assets; удалить stale gitlink уже удалённого `antonov-andrey-org/submodule-anthropic-agent`; перенести reusable task workflows в `agent-workflows@agent-plugins`, перенести workflow-container procedures в `workflow-container-agent-tools@agent-plugins`, вынести reusable opinionated engineering standards в отдельный provider `project-standards@project-standards`; удалить consumer-дубли только после доказанного provider cutover; сохранить project-specific contracts и поведение.

## Source Contracts

- `.spec/2026-07-23-project-standardization-spec.md`: полный multi-repository migration, cutover и acceptance contract.
- `DESIGN.md`: stable artifact ownership, GitHub repository lifecycle, `agent-plugins`, `project-standards`, project-local boundary, configuration и verification model.
- `AGENTS.md`: действующие ограничения coordinating repository.
- `plugins/agent-workflows/skills/goal-brainstorm/references/specification-contract.md`: harness-neutral specification, goal lifecycle и terminal completion audit.
- Существующие применимые `AGENTS.md` затронутых repositories: project-specific implementation и verification rules; paired specification владеет созданием отсутствующих корневых `AGENTS.md`.

## Constraints

Работать provider-first и сохранять unrelated user changes. До удаления consumer copies доказать availability в fresh session для `agent-workflows`, `marketplace-agent-tools`, `workflow-container-agent-tools` и `project-standards`; missing required provider должен останавливать project mutation. Не создавать generated copies standard prose в project `AGENTS.md`. Не обновлять runtime versions, application behavior, production state или business contracts только ради стандартизации. По отдельному прямому требованию пользователя `bin/venv_create.sh` в `marketplace-tools`, `marketplace-next-co-uk`, `marketplace-parser` и `marketplace-tr` должен воспроизводимо создавать проверочный environment на уже объявленном Python `3.14`, проверять major/minor без patch pin и не менять dotenv loading или application bootstrap. Эта goal явно разрешает создать `antonov-andrey/project-standards`, переименовать `antonov-andrey/workflow-container-tools` в `antonov-andrey/agent-plugins` и удалить exact repository `antonov-andrey-org/template-bin` после `Batch 37`; другие checkout, repository, branch и remote не удалять или переименовывать без отдельного exact approval. Clean local orphan `marketplace-tools-ml` не входит в scope и не удаляется этой goal. Для retired `compose-codex-app-server` разрешён только exact Docker cleanup по `Batch 36`; GitHub repository и local checkout уже отсутствуют и не должны воссоздаваться. Уже удалённые `antonov-andrey-org/workflow-source-codex` и `antonov-andrey-org/submodule-anthropic-agent` не удалять повторно и не воссоздавать; удалить только stale local gitlink по `Batch 38`. Перед remote deletion доказать scope `delete_repo`, не раскрывая credentials. Не создавать compatibility aliases, dual-provider steady state, replacement template, пустые design artifacts или tracked task history. Commit и push выполнять только как необходимые части явно разрешённых repository lifecycle actions и coordinated consumer cutover; другие publication actions требуют отдельного exact approval.

В `Legacy` consumers не изменять ради provider convergence существующие `load_dotenv()` и `config_env` calls, dotenv file order или `override` semantics, environment keys, application bootstrap, а также `config_env`, `config_logging` и `config_sqlalchemy` gitlinks и call sites, если их cutover требует такого изменения. `Batch 24`, `Batch 25` и `Batch 26` разрешают только перечисленные в них consumer cutover, включая точечно названные `Legacy` owners, при сохранении этой environment/bootstrap-семантики и project-owned verification. Любой другой `Legacy` consumer, включая `scrapy-next-deprecated`, остаётся вне provider convergence scope и требует нового явного запроса пользователя.

Выполнить `Batch 39` как обязательную часть repository rename: заменить old SSH, HTTPS и equivalent Git URLs во всех active projects и configuration; переименовать primary checkout, все live local checkout/worktree/marketplace copies и все plugin-source/cache copies в canonical target names; repository copies должны называться `agent-plugins`, а installable-plugin copies — соответствующими plugin identifiers. Не переписывать Git history, reflogs, completed logs или immutable task history ради удаления historical old name.

Выполнить `Batch 31` как exact allowlist retirement: удалить только старый `marketplace-tr-priority` brand-size-chart execution contour и два skill без successor, не затрагивая `ParsedOfferProductSizeChartImage`, `ParsedOfferProductSizeChartParse`, `ParsedOfferProductOzonSize`, Ozon size references, `trendyol-size-chart-parse` и действующий Trendyol-to-Ozon size pipeline. Не выполнять migration или physical DB-table drop; будущая интеграция результата `brand-size-chart` с MTP требует отдельного product contract.

Классифицировать каждый skill как переносимый либо project-local только по явному решению пользователя, без автоматического consumer-count criterion. Полный `marketplace-tr-priority/.codex/skills/trendyol-size-chart-parse` остаётся project-local; `marketplace-agent-tools` не получает его копию или replacement, а generic batch dependency предоставляется через `agent-workflows:sequential-batch`.

Полностью сохранить project-local `marketplace-tr-priority` skills `ozon-attribute-map`, `ozon-data-contract` и `ozon-dimension-reference`, их owner-local assets, project data contracts и production consumers по `Batch 35`; не создавать их copies или replacements в `marketplace-agent-tools`.

Выполнить `Batch 33` как полное удаление неиспользуемого Python package `workflow_container_tools`, его CLI, package-specific tests, wheel surface и operator references без replacement discovery interface.

Выполнить `Batch 34` как targetless retirement нереализованного `workflow-control-center/doc/draft/ozon-product-card-fill-skill.md` без replacement task artifact, skill, plugin asset или workflow project.

Выполнить `Batch 36` как оставшийся exact Docker cleanup `compose-codex-app-server`: удалить только stopped container, image, legacy volume и empty network; не воссоздавать repository или checkout, не переносить skills, application code, contracts или runtime data и не затрагивать другие Docker artifacts.

Выполнить `Batch 37` как retirement `template-bin`: сохранить у каждого из шести consumers все current `bin/**` runtime files побайтно и с прежними modes, удалить только obsolete `bin/readme.txt`, active template references и два configured local remotes, не переписывать Git history и не создавать replacement shared owner. Удалять checkout и GitHub repository только после consumer verification.

Выполнить `Batch 38` как exact cleanup уже удалённых repositories: не предпринимать действий для `workflow-source-codex`, кроме no-recreate verification; удалить из `marketplace-next-co-uk` только tracked gitlink `anthropic_agent`, его `.gitmodules` section и local submodule metadata, сохранив независимую `textgrad_engine` Anthropic integration; не выполнять повторную remote deletion.

Не изменять автоматически `workflow-control-center/AGENTS.md` или `marketplace-tr-priority/AGENTS.md`. Каждое их сокращение, объединение, переформулирование или перемещение выполняется только из закрытого approved source-to-target ledger; преобразования вне него запрещены.

## Verification

Выполнить весь `Verification design` и каждый acceptance contract из paired specification, применимые проверки всех изменённых repositories и финальный workspace-wide semantic review. Для protected instruction migrations доказать полное соответствие final owners каждому approved ledger entry; одного уменьшения line count или общего semantic audit недостаточно. Для каждого затронутого `Legacy` consumer доказать changed-file inventory и semantic diff, не содержащие запрещённых environment-loading и зависимых provider-cutover изменений. Для `Batch 31` доказать exact deletion inventory, отсутствие старых brand-size-chart references, сохранение всех protected persisted entities, успешные targeted Trendyol-to-Ozon size tests, полный protected-project handoff suite и независимую validation project `brand-size-chart`. Для `Batch 32` доказать сохранение полного project-local `trendyol-size-chart-parse` и его behavior при удалении только generic consumer-local agent mechanics. Для `Batch 36` доказать отсутствие exact Docker artifacts и active references без replacement retired assets. Для `Batch 37` доказать сохранение consumer-local runtime files, удаление active template relations и отсутствие replacement template. Для `Batch 38` доказать отсутствие stale anthropic gitlink и сохранение независимой `textgrad_engine` integration без повторной remote deletion. Для `Batch 39` доказать canonical GitHub identity и `origin`, отсутствие old active Git URLs и live local owner paths, корректное разделение repository и plugin directory names и fresh-session discovery target plugins без old provider. Финальный inventory должен содержать 18 existing canonical repositories и `antonov-andrey/project-standards`, всего 19 canonical repositories. Завершение требует опубликованных и independently verified `antonov-andrey/agent-plugins` и `antonov-andrey/project-standards`, fresh-session discovery четырёх installable plugins, разрешимости каждого `Required Standards` skill, отсутствия consumer-дублей, generated standard prose и obsolete artifact paths и сохранения всех project-specific requirements. После предполагаемого завершения выполнять полный audit текущего scope с нуля против этой goal, paired specification, всех stable source contracts и фактического repository и external state; при любой незавершённой, противоречивой, устаревшей или непроверенной находке сохранять goal active, исправлять все находки, повторять затронутую verification и начинать новый полный audit с нуля. Goal можно завершить только после нового полного аудита без находок после последнего исправления и при продолжающемся успехе всей required verification; отдельный completion ledger, evidence document или completion report не создаётся. По прямому требованию пользователя от 24 июля 2026 года текущая `.spec/2026-07-23-project-standardization-{spec,goal}.md` pair сохраняется после завершения и не удаляется как completed task artifact.
